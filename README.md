# create-home

create-home cli help with create home service and app
(clone of create-vite)

## Development

- Install dependencies:

```bash
vp install
```

- Run the unit tests:

```bash
vp test
```

- Build the library:

```bash
vp pack
```
